language: python

python:
    - 2.7
    - 3.4
    - 3.5
    - 3.6

cache:
    pip: true

install:
    - pip install -r requirements.txt
    - pip install coveralls

script:
    - pytest -n 12 --cov=energydram

after_success:
    - coveralls

